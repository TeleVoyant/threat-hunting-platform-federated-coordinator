# Docker-secret mount target. Place real secrets here (git-ignored):
#
#   fl_jwt_secret   — operator JWT signing key, >=32 random bytes:
#                       openssl rand -base64 48 > secrets/fl_jwt_secret
#
# Never commit the actual secret files. Only this .gitkeep is tracked.
